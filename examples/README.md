# Examples

| Script | Shows | Needs server? |
|---|---|---|
| — | The plugin has no runnable example of its own: what it does is visible only in Psalm's output. The fixture project under `tests/Integration/Fixtures/Matchers` is the executable demonstration, and it runs as part of `make build`. | no |
